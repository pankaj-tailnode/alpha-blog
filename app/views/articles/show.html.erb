<h1>Showing article details</h1>
<p><strong>Title: </strong><%= @article.title %></p>
<p><strong>Description: </strong><%= @article.description %></p>

<td><%= button_to 'Edit', edit_article_path(@article.id), method: :get %></td>
<td><%= button_to 'Delete', article_path(@article.id), method: :delete, onclick: "return confirm('Are you sure?')" %></td>
<td><%= button_to 'Return to articles listing', articles_path, method: :get %></td>